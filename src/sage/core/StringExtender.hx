package sage.core;

/**
 Extends the core functions of the String class and also has additional 
 constants useful for working with strings.
 **/
class StringExtender {
    /**
     Represents a NULL character. 
     **/
    public static inline var NULL_CHAR = 0x00;
}